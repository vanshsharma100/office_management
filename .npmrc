# Prisma prepares its query engine in a postinstall step. Managed hosts often
# install with scripts disabled, which leaves the engine missing or without the
# execute bit and fails the build with an unhelpful binary error.
ignore-scripts=false

# Deliberately no `script-shell` here. Setting it to a Unix shell path would
# break every npm script on the Windows machines this is developed on.
