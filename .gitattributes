# These files run on Linux. Windows checkouts must not turn their line endings
# into CRLF, or the server refuses to execute them.
*.sh text eol=lf
Dockerfile text eol=lf
Caddyfile text eol=lf
*.yml text eol=lf
